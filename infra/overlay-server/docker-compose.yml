services:
  mongodb:
    image: mongo:6.0.28@sha256:8b6d8f5bbedb25cb73517b65cf99f13aeb75ad5b157a56c479287a840bbad3ac
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME:-root}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD:?set MONGO_ROOT_PASSWORD}

  mysql:
    image: mysql:8.4.10@sha256:8dbcf531a03aade657e181b9cf2f1d1803ce621a1d55610cb44cb531ab7d7db6
    container_name: mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:?set MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-appdb}
      MYSQL_USER: ${MYSQL_USER:-appuser}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:?set MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql

  overlay-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: overlay-server
    depends_on:
      - mongodb
      - mysql
    ports:
      - "8080:8080"
    environment:
      MONGO_URL: mongodb://${MONGO_ROOT_USERNAME:-root}:${MONGO_ROOT_PASSWORD:?set MONGO_ROOT_PASSWORD}@mongodb:27017
      KNEX_URL: mysql://${MYSQL_USER:-appuser}:${MYSQL_PASSWORD:?set MYSQL_PASSWORD}@mysql:3306/${MYSQL_DATABASE:-appdb}
      SERVER_PRIVATE_KEY: ${SERVER_PRIVATE_KEY:?set SERVER_PRIVATE_KEY}
      WALLET_STORAGE_URL: https://store-us-1.bsvb.tech
      HOSTING_URL: https://your.overlay.network
      ARC_API_KEY: ${ARC_API_KEY:-}
      NETWORK: main
      NODE_NAME: your-overlay-node
      ADMIN_TOKEN: ${ADMIN_TOKEN:?set ADMIN_TOKEN}
      GASP_ENABLED: "false"
      OVERLAY_CORS_MODE: ${OVERLAY_CORS_MODE:-}
      OVERLAY_CORS_ALLOWED_ORIGINS: ${OVERLAY_CORS_ALLOWED_ORIGINS:-}
      OVERLAY_CORS_ALLOWED_HEADERS: ${OVERLAY_CORS_ALLOWED_HEADERS:-}
      OVERLAY_CORS_EXPOSED_HEADERS: ${OVERLAY_CORS_EXPOSED_HEADERS:-}
      OVERLAY_CONTENT_SECURITY_POLICY: ${OVERLAY_CONTENT_SECURITY_POLICY:-}
      OVERLAY_CROSS_ORIGIN_RESOURCE_POLICY: ${OVERLAY_CROSS_ORIGIN_RESOURCE_POLICY:-}
      OVERLAY_CROSS_ORIGIN_OPENER_POLICY: ${OVERLAY_CROSS_ORIGIN_OPENER_POLICY:-}
      OVERLAY_FRAME_OPTIONS: ${OVERLAY_FRAME_OPTIONS:-}
      OVERLAY_PERMISSIONS_POLICY: ${OVERLAY_PERMISSIONS_POLICY:-}
      OVERLAY_STRICT_TRANSPORT_SECURITY: ${OVERLAY_STRICT_TRANSPORT_SECURITY:-}
      # OpenTelemetry — point at any OTLP/HTTP collector. Unset => console exporters.
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_HEADERS: ${OTEL_EXPORTER_OTLP_HEADERS:-}
      OTEL_SERVICE_NAME: ${OTEL_SERVICE_NAME:-overlay-server}
      OTEL_RESOURCE_ATTRIBUTES: ${OTEL_RESOURCE_ATTRIBUTES:-}
      DEPLOY_ENV: ${DEPLOY_ENV:-production}
    # Direct node invocation with the OTel preload (image has no npm; see Dockerfile).
    command: ["node", "--import", "./dist/telemetry.js", "dist/index.js"]

  janitor:
    image: alpine:3.24.1@sha256:28bd5fe8b56d1bd048e5babf5b10710ebe0bae67db86916198a6eec434943f8b
    container_name: janitor
    depends_on:
      - overlay-server
    environment:
      ADMIN_TOKEN: ${ADMIN_TOKEN:?set ADMIN_TOKEN}
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache curl
        echo '0 0 * * * curl -X POST -H "Authorization: Bearer ${ADMIN_TOKEN}" http://app:8080/admin/janitor' > /etc/crontabs/root
        crond -f -l 2

volumes:
  mongo_data:
  mysql_data:
